import { moduleMetadata } from '@storybook/angular';
import { Story, Meta, ArgsTable } from '@storybook/addon-docs';
import {SepttwensevenComponent} from '../../app/youtube/Sep2022/septtwenseven/septtwenseven.component';
import { linkTo } from '@storybook/addon-links';
import { MarkdownModule, MarkdownService } from 'ngx-markdown';
import { HttpClient, HttpClientModule } from '@angular/common/http';
import { AppSharedModule } from '../../app/app-shared/app-shared.module';

# Use Zerodha API to access the trading platform

<Meta title="Youtube27sep22/RunAngular"   
  parameters={{
      viewMode: 'docs',
      previewTabs: { 
        canvas: { hidden: true } 
      },
  }}
  decorators={[
moduleMetadata({
  declarations: [SepttwensevenComponent],
      imports: [
        AppSharedModule,
        HttpClientModule,
        MarkdownModule.forRoot({ loader: HttpClient }),
      ],
      providers: [MarkdownService],
    }),
    
    ]} />


<Story name="RunAngular">
  {{
   template: `<app-septtwenseven [myselectedtemp]= "myselectedtemp"></app-septtwenseven>`,
    props: {
    myselectedtemp : 'RunAngular'
    },
  }}
</Story>