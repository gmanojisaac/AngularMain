import { moduleMetadata } from '@storybook/angular';
import { Story, Meta, ArgsTable } from '@storybook/addon-docs';
import {OctfifthComponent} from '../../app/youtube/Oct2022/octfifth/octfifth.component';
import { linkTo } from '@storybook/addon-links';
import { MarkdownModule, MarkdownService } from 'ngx-markdown';
import { HttpClient, HttpClientModule } from '@angular/common/http';
import { AppSharedModule } from '../../app/app-shared/app-shared.module';


## OneSideView Application should be run in the Android Phone
check the android version from the settings -> About Phone

# Plan for a single trade per day with 13% total profit, so 10% after tax and charges.
Once the profit is achieved call it a day and shut down the terminal.

<Meta title="Youtube05oct22/OneSideView"   
  parameters={{
      viewMode: 'docs',
      previewTabs: { 
        canvas: { hidden: true } 
      },
  }}
  decorators={[
moduleMetadata({
  declarations: [OctfifthComponent],
      imports: [
        AppSharedModule,
        HttpClientModule,
        MarkdownModule.forRoot({ loader: HttpClient }),
      ],
      providers: [MarkdownService],
    }),
    
    ]} />


<Story name="OneSideView">
  {{
    template: `<app-octfifth [myselectedtemp]= "myselectedtemp"></app-octfifth>`,
    props: {
    myselectedtemp : 'OneSideView'
    },
  }}
</Story>